{%- if title -%}
    {%- set pageTitle = title -%}
{%- else -%}
    {%- set pageTitle = site.title -%}
{%- endif -%}
<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <!-- <link rel="preload" href="/web/css/fonts/lato/2.0/LatoLatin-Regular.supersubset.woff2" as="font" type="font/woff2" crossorigin=""> -->
    <title>{{ pageTitle }}</title>
    <meta property="og:title" content="{{ pageTitle }}">
    <meta name="author" content="{{ author }}">
    <meta property="og:site_name" content="Joe Komskis">
    <meta name="description" content="Joe Komskis's personal website">
    <meta property="og:description" content="Joe Komskis's personal website">
    <link rel="canonical" href="https://www.jkomskis.com">
    <link rel="stylesheet" href="/assets/css/main.min.css" type="text/css">
</head>

<body>
    {{ content | safe }}
</body>

</html>